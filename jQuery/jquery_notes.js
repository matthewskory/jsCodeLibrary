.append(), appendTo()  //puts stuff at the end (what/where your adding)
.prepend(), prependTo() // adds stuff at the beginnig(what/where your adding)

.after(), .before() //adds tag before or after target element (what your adding)


.remove() // deletes the element it's called on (no args)
.empty() // deletes an elements content AND the content of all it's dependents (non args)

.addClass('classNamein quotes'), .toggleClass('classnameinquotes') // adds css class to selected element
.css("cssProp", "cssPropVal")

.html("new html added here") //sets the value of the selector supplied
.val()//gets the value from the element selected